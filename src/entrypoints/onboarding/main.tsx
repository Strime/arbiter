import React from 'react';
import ReactDOM from 'react-dom/client';
import { OnboardingApp } from '../../features/preferences/presentation/onboarding/onboarding-app';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <OnboardingApp />
  </React.StrictMode>,
);
